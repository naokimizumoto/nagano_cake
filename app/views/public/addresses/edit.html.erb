<%= form_with model: @address, local:true do |f| %>
  <h4>郵便番号（ハイフンなし）</h4>
  <%= f.text_field :postal_code %>
  <h4>住所</h4>
  <%= f.text_field :address %>
  <h4>宛名</h4>
  <%= f.text_area :name %>

  <%= link_to "変更内容を保存"　addresses_path %>